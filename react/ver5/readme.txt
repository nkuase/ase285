## To create the files. 

mkdir react-express-app
cd react-express-app
npm init -y
npm install express
npx create-react-app client

## When we don't want to create files.

### Server
npm i // to install the packages
nodemon server2.js // start the server

### React
npm i // to install packages
npm run build // to make the build directory